import React from "react";
import classNames from "classnames";

// This component is used to skip to with the button in the Splash component

export function ScrollTarget() {
  return <div id="afterSplash" />;
}
